const heading = React.createElement("h1",{id:"abc"},"hello world from React");
    const root = ReactDOM.createRoot(document.getElementById("root"))

    root.render(heading);   
console.log(heading) 
// console.log return object
    // hello world from react using react